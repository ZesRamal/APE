import React from "react";

const AppBarSpacer = ({children}) => {
    return(
        <div className="appBarSpacer min-h-[5.6rem]">
            {children}
        </div>
    );
};

export default AppBarSpacer