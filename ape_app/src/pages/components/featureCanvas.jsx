import React from 'react';

const FeatureCanvas = ({children}) => {
    return(
        <div className='featureCanvas bg-white h-full w-full relative'>
            {children}
        </div>
    )
}
export default FeatureCanvas;