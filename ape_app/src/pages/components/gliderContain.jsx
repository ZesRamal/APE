import React from "react";

const GliderContain = ({children}) => {
    return(
        <div className='w-full my-0 mx-auto relative'>
            {children}
        </div>
    );
}

export default GliderContain;
